import StrictLeanPolicy.Decision
import StrictLeanPolicy.ResultState
import StrictLeanPolicy.Claim

/-! Claim and result admission interfaces. ResultState carries the invariant at every
insertion. An Accepted policy result and its semantic soundness/completeness theorems
are subsequent deliverables; no Boolean success alias is exported here. -/
