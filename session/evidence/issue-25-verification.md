# Issue 25: native observation and feedback

Scoped delivery evidence for shared observation, native feedback and the execution-root
repair. This is not full semantic conformance, whole-project Accepted assembly or a
completed twenty-rule product. The [inventory](issue-25-inventory.json) records actual
Lake modules, declarations, transitive axiom sets and pure-policy theorem types. Display
names and input hashes are observations, not admission keys or authenticity proofs.

[Focused qualification output](issue-25-qualification.txt) records the actual native,
public-adopter, root-mutation and forced-import results.

## Pins and checks

- Lean 4.33.1, compiler `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`;
  root Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`.
- Unpartitioned `./scripts/verify.sh`: PASS, 162.10 seconds under the hard 420-second
  deadline. Includes registry/CLI qualification, 36 native controls, fresh source
  elaboration/kernel admission and 70 positive, 23 negative and one teaching fence.
- Census: 29 owned modules, 4,041 declarations, including 17
  StrictLeanPolicy modules and 3,531 policy declarations. Exact per-declaration
  foundations are in the inventory. All observed axioms are among propext, Quot.sound and Classical.choice.
- `checkerSelftest --policy-domain-only`: PASS in 111.78 seconds, transport and actual public-adopter
  controls. Ordinary and all three tagged unused extern roots fail for the intended
  execution-boundary reason; genuine/forged helper and present-IR placeholder controls
  remain. Every mutation requires fresh successful restoration. Forbidden Report/Probe
  controls are retained. These are scoped operational observations, not universal proofs.
- `checkerSelftest --native-adopter-only`: PASS, the production native import through
  the public project checker in an external Core/Lean adopter (6.94 seconds).
- `checkerSelftest --forced-collector-only`: PASS, fresh original-manifest positive,
  an excluded collector source import rejected for its intended reason, and restored
  positive (142.43 seconds). No manifest is narrowed to manufacture that result.

The first ordinary run failed in 88.43 seconds: after extraction, Probe's forced collector
import was misclassified as a source import of an excluded library. The repair binds
its exact checker artifact and distinguishes force-only presence from non-probe source
imports. Actual source imports retain exclusion checks; Collect itself is still scanned
for forbidden Report/Probe imports. The native scope guide describes this boundary.

The optional forced-collector diagnostic setup was corrected after the ordinary run and
separately compiled and qualified; operational, policy, native, registry and fence inputs
were unchanged. The ordinary evidence is reused for those unchanged claims. Exact-head
CI runs the complete ordinary command again on the final delivery.

## What was established

- Shared actual-environment record construction preserves structural identities,
  ownership, ranges, axioms and contract observations. Snapshot collection omits replay
  and generated-role authentication; replayCandidate remains an observation stage.
- Native legacy and module-system clients initialize the actual meta callbacks and
  persistent material tag. Command selection is partial; completed-module enumeration
  includes private, generated, unused and binder-less constants.
- The native bridge reuses executed policy and a single failure-to-ID mapping. Real-source
  controls check structured codes, genuine source positions, module/configuration hosting,
  warning promotion, scoped options, pending-role and invalid-coordinate reasons, and
  Markdown/Verso/current/imported/inherited/private documentation behavior.
- Unsupported configuration refuses each affected command. The supported language
  frontend owns imports-only fallback, immutable snapshots and cancellation. No independent
  VS Code stale/cancellation/widget interaction claim is made; that remains #14.
- Positive/restored native controls permit no unexpected diagnostics. Hole, recovered-error
  and compiler-trust negatives explicitly admit their known original compiler messages.
- Eligible roots survive auxiliary/no-confusion/matcher tags. Only absent initial standalone
  IR is relaxed; source/boundary/available-IR and retained compiler-call checks remain.
  Tags establish neither authentic generation nor independent callable machine code.

Pure policy theorem statements, proof bodies and receipt fields are unchanged. Module
interfaces expose existing definitions for downstream proof reduction. Raw supplied-set
helpers do not authorize roles or acceptance; policyFor/foundationFor still require
inventory-bound Roles. Compiler observations and IO are trusted operational boundaries,
not proved by those logical theorems. The operational StrictLean library remains explicitly
excluded from the conforming proof surface; its relevant behavior is qualified separately.

## Independent review and remaining scope

Independent reviewers covered shared collection/native behavior, module/proof interfaces,
execution roots and the forced-import distinction. Focused reviews closed diagnostic tagging,
warning promotion, invalid configuration, scoped options, overadvertised modes, malformed
range/qualification reasons, present-IR placeholders, raw-API overclaims and stale handoffs.
The five successor issue-body edits received separate semantic-preservation review.

Affected concerns include COMP-03, DECL-03, DOC-01/02, BUILD-03, DOGFOOD-03 and focused
MUT-02/04 controls; module/proof review covers changed public visibility and preserved
proof/receipt semantics. These scoped reviews do not assert every chapter-9 row across
all surfaces. Exact-head hosted CI and signed integration are separate gates on the PR.

#13 retains complete twenty-rule examples and expected outcomes; #7 mandatory global
jobs and Accepted composition; #14 Lake driver/configuration explanation and real editor
journeys; #15 the deployed website. Documentation presence does not establish registration
completeness or substantive R-DOC adequacy. No optional graph or unrun broad campaign is
reported PASS. Lean API reuse introduces no con-leche code dependency or blanket attribution.
